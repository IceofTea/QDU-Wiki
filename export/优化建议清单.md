# 青岛大学 Wiki 腾讯文档 — 优化建议清单

生成时间：维护任务阶段
说明：本次核对源站 `docs/` 与导入腾讯文档的内容，找出可优化处。以下分「已自动完成」「需手动处理」两类。

## 一、已自动完成（本地生成文件已修正）

修复了转换脚本 `gen_children.js` / `gen_indexes.js` 的一个 bug：**admonition 引用块内的相对 `.md` 链接未转纯文本**（普通行转换了，块内行漏了）。

- 全部子页面 + 板块索引页的**相对 `.md` 链接**已转为纯文本（如 `[报到校区与学院联系方式](campus_registration.md)` → `报到校区与学院联系方式`）。
- **外部 URL 链接正确保留**（如 GitHub 的 `README-zh_CN.md`、StackOverflow 等，腾讯文档中可正常点击）。
- 修正后的文件已就绪于 `export/children/`，可随时用于重新导入。

## 二、腾讯文档内需处理（正文无法自动编辑）

腾讯文档**正文编辑无法自动化**（此前已确认所有方式均不持久），因此文档内已有的无效相对 `.md` 链接只能**手动删除**或**删除重导**。

### A. 已导入的 7 个子页面（正文含无效相对 .md 链接）
1. 新生手册 / 入学准备
2. 新生手册 / 本科生手册
3. 新生手册 / 报到校区与学院联系方式
4. 校园服务 / 校园卡与证件
5. 校园服务 / 网络服务
6. 生活指南 / 医疗
7. 学习学业 / 学业制度

**处理方式（二选一）**：
- 手动：在这些页面正文中，把形如 `[文字](xxx.md)` 的片段删去 `(xxx.md)` 括号部分，只留 `文字`。
- 重导：删除该子页面后用 `export/children/` 下的修正版重新导入（注意：重导后页面会出现在树顶部，需重新拖回原板块）。

### B. 板块索引页（你已手动粘贴的内容）
关于 Wiki、文件共享、有话送你、学院详情 四个板块页内，有指向「维护说明 / 说明 / 查看详情」的相对 `.md` 链接。修正版已生成于 `export/children/INDEX/`，可对照替换链接部分。

## 三、无需处理
- 主页《青岛大学 Wiki 校园指南（关键字检索版）》：内容完整，外部链接正常。
- 各子页面内容与源站 `docs/` **完全一致**（admonition 已正确转引用块，正文逐字核对无出入）。

## 四、重导的技术提示（重要）
自动删除「子页面」在技术上不可靠：子页面节点在树中为非标准渲染，稳定定位与右键删除机制不明确；且删除后重导会破坏你已经拖好的 66 个子页面层级。**因此不建议我自动批量重导**，上述 7 个页面的 `.md` 链接建议由你按需手动处理（改起来只是删几个括号，很轻量）。
