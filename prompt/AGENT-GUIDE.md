# Agent 维护指南（AGENT-GUIDE）

> **写给每一位将要维护本站的 AI Agent：**
> 在动手修改任何文件之前，请**完整阅读本说明**。本文件描述了本站的「目的」「架构」「逻辑」「维护方法」以及「Agent 维护工作流」，是你在本站工作的总纲。
>
> 请把本文件当作你的「入职培训」。遇到不确定的规则时，以本文件为准；本文件未覆盖的部分，遵循仓库中已有文件的风格与惯例。

---

## 一、项目目的

**青岛大学 Wiki（QDU-Wiki）** 是一份**非官方**的校园生存指南，由百度贴吧【青岛大学吧】的吧友与热心校友共同维护。

- **服务对象**：青岛大学新生（主要）与老生
- **核心价值**：帮新生完成从「拿到录取通知书」到「融入校园生活」的全流程过渡——报到准备、校区选择、军训、选课、考试、宿舍、餐饮、社团、组织关系等
- **内容风格**：说人话、讲干货、接地气。允许轻松幽默（~~删除线吐槽~~、emoji），但要确保信息准确、对新生有用
- **重要定位**：是「新生手册」性质的内容站，不是学校官网的复刻。所有内容都应体现「过来人的经验与信息差」

> **对 Agent 的第一条要求**：所有修改都必须服务于「帮助新生/老生更好地了解青大」这一目的。不要为了形式而堆砌内容，也不要写官腔。

---

## 二、项目架构

### 2.1 技术栈

| 项目 | 说明 |
| --- | --- |
| 静态站点生成器 | **MkDocs** + **Material** 主题 |
| 内容形式 | 纯 Markdown（`docs/` 下所有 `.md` 文件） |
| 导航 | `mkdocs.yml` 中的 `nav:` 字段 |
| 样式 | `docs/stylesheets/extra.css`（自定义） |
| 交互 | `docs/javascripts/home-hero.js`、`docs/javascripts/season-switcher.js` |
| 部署 | GitHub Actions（`.github/workflows/ci.yml`）自动构建并部署到 GitHub Pages |
| 数据存储 | 内容全部内嵌在 Markdown 中；大文件（资料 PDF、图片等）走外部网盘链接 |

### 2.2 目录结构

```
QDU-Wiki/
├── mkdocs.yml                    # 站点配置 + 导航定义（重要！）
├── README.md                     # 面向人的仓库说明
├── prompt/                       # 维护文档统一目录
│   ├── prompt.md                 # 面向社区的「提示词工程」提交模板
│   ├── AGENT-GUIDE.md            # 本文档（Agent 规则总纲）
│   └── AGENTS.md                 # 本站维护须知速览版（根目录入口指向此处）
├── docs/                         # 全部站点内容
│   ├── index.md                  # 首页（含 Hero 手册 + 学校概况）
│   ├── about/                    # 关于 Wiki（index + guide 维护说明）
│   ├── new/                      # 新生手册（入学准备/防诈骗/军训/联系方式/报到校区）
│   ├── live/                     # 生活指南（餐饮/住宿/交通/医疗/设施/购物）
│   ├── study/                    # 学习学业（制度/校历/入党/考试/官方文件子目录）
│   ├── service/                  # 校园服务（网络/一卡通证件）
│   ├── college/                  # 学院详情（每学院一个 md + 索引页）
│   ├── organization/             # 学生组织
│   ├── share/                    # 文件共享
│   ├── words/                    # 有话送你（学长学姐寄语）
│   ├── assets/                   # 全局资源（logo、四季图片、favicon）
│   ├── pics/                     # 配图（按板块分子目录）
│   ├── stylesheets/extra.css     # 自定义样式
│   └── javascripts/              # 自定义 JS
├── site/                         # 构建产物（gitignore，不手动改）
└── .github/workflows/ci.yml      # CI 自动部署
```

### 2.3 核心配置文件 `mkdocs.yml`

- `nav:` 定义了全站导航结构。**新增或移动页面后必须同步修改 nav**，否则新页面无法访问
- `theme:` 使用 Material，开启了 `navigation.tabs`、`navigation.instant`、`navigation.indexes` 等
- `markdown_extensions:` 启用了 `admonition`、`pymdownx.tabbed`、`pymdownx.details`、`attr_list`、`md_in_html`、`pymdownx.superfences` 等，意味着这些语法在站内可用
- `plugins:` 使用 `search` + 两个 git 插件（CI 环境外默认关闭）
- `extra_css` / `extra_javascript` 引用了自定义样式与脚本

> **对 Agent 的要求**：修改 `mkdocs.yml` 时务必保持 YAML 缩进正确（每级 2 空格），不要删除或重排已有导航项，只做「追加」或「在明确要求下的局部调整」。

---

## 三、页面逻辑与内容惯例

### 3.1 导航与板块

全站 9 大板块（导航顶层）：
1. 主页
2. 新生手册 `new/`
3. 生活指南 `live/`
4. 学习学业 `study/`
5. 校园服务 `service/`
6. 学院详情 `college/`
7. 学生组织 `organization/`
8. 文件共享 `share/`
9. 有话送你 `words/`
10. 关于 Wiki `about/`

### 3.2 常见页面结构

- **正文以 `# 页面标题` 开头**（H1），小节用 `##`、`###`
- 善用 Material 的提示框语法（详见下节）
- 需要「待补充」的地方，用 `!!! warning "待更新"` 或 `!!! info` 标注，方便后续维护者搜索定位
- 内容性信息尽量用**表格**呈现（对比、清单、联系方式等），可读性高
- 涉及政策/规则类的页面，通常在开头用 `!!! tip "文件依据"` 注明出处文件与文号（如「青大教字〔2021〕21号」），并加一句「如有变动以官方最新文件为准」

### 3.3 提示框（Admonition）用法

| 类型 | 语法 | 用途 |
| --- | --- | --- |
| 注意 | `!!! note "标题"` | 一般性说明 |
| 提示 | `!!! tip "标题"` | 过来人经验、小技巧 |
| 警告 | `!!! warning "标题"` | 重要提醒 |
| 危险 | `!!! danger "标题"` | 严重警示（如作弊、诈骗） |
| 引用 | `!!! quote "标题"` | 引言、备注、署名 |
| 成功 | `!!! success "标题"` | 已完成 / 好消息 |

用法示例：

```markdown
!!! tip "省钱技巧"
    家乡离青岛较远的同学能省不少钱，记得利用好这次优惠。
```

### 3.4 链接与图片路径

- **相对路径**：站内互相引用一律用相对路径。`docs/new/x.md` 引用同目录文件写 `y.md`；引用 `docs/college/` 下文件写 `../college/y.md`
- **图片**：统一放 `docs/pics/` 下对应板块子目录（`pics/new/`、`pics/live/` 等），引用路径随页面所在目录变化（根目录页面 `docs/index.md` 用 `pics/xxx.png`，子目录页面用 `../pics/xxx.png`）
- **命名**：`<板块>-图<编号>-<描述>.png`，如 `study-图1-校历.png`
- **外部链接**：直接写完整 URL

### 3.5 中英文排版

中英文之间、中文与数字之间加一个空格（如「使用 MkDocs 构建」「2026 级新生」），读起来更舒服。这与仓库已有页面保持一致。

### 3.6 学院详情页 `college/`

- `index.md` 是学院索引页，按「医学类 / 理工类 / 人文社科类 / 经管类 / 艺术类 / 继续教育类」分组，表格列出各学院、简介、状态、详情链接
- 每个学院一个独立 `.md`，文件名用英文（如 `computer_science_and_technology.md`、`school_of_medicine.md`）
- 模板结构：学院简介 → 专业设置 → 特色培养 → 校区安排 → 学习与竞赛建议 → 保研与就业 → 常见问题 → 贡献
- 未维护的学院页保持模板占位（`待更新`），已维护的（如计算机科学技术学院）内容充实

> **对 Agent 的要求**：新增学院页面时，先看看同目录已有页面的模板结构，保持一致。

!!! warning "关于「未完成维护的学院页」"
    若某学院页仅为后续维护预留（内容基本是模板/待更新），**只新建文件即可，不要加入 `mkdocs.yml` 的 nav**，也不要挂到 `college/index.md` 的列表里。已完整维护的学院才进入导航与索引展示。这样既能占位方便后续补充，又不会让访问者点进空白页。

---

## 四、维护方法

### 4.1 三种维护来源

本站信息主要来自三处，Agent 都要能处理：

1. **社区提交的 `prompt.md` 提示词**：社区成员按「定位 + 原文 + 修改内容 + 显示格式」的格式写入修改需求。Agent 据此定位文件、替换内容。
2. **直接上传的信息源**：图片（截图/海报）、文档（PDF/Word）、聊天记录等原始素材，需要 Agent 提炼文字、整理成结构化 Markdown。
3. **明确的修改指令**：维护者直接告诉 Agent 改哪里、怎么改。

### 4.2 信息源的处理方式

- **图片**：用 OCR（如 RapidOCR）提取文字，再整理成 Markdown 表格/列表
- **长文档**：提炼关键信息，按板块拆分补充到对应页面，避免整篇堆砌
- **口述/回忆录式文字**：整理润色后按页面结构写入

> **原则**：信息源是「原材料」，Agent 的职责是**加工成符合本站风格与结构的 Markdown 内容**，而不是原样粘贴。

### 4.3 自主采集信息源（无需人工投喂）

生活板块（餐饮/住宿/交通/医疗/设施/购物）信息容易过时，且官网首页看不到、论坛信息太脏。Agent 应能**自己联网采集新鲜信息**。以下信息源已实测可用（截至 2026-07）：

**官方源（权威、可直接抓取，优先采信）**

| 信息源 | URL | 可挖到的内容 |
| --- | --- | --- |
| 后勤·饮食服务 | `houqin.qdu.edu.cn/fwzn/ysffw.htm` | 食堂名单、营业时间、膳食部电话 |
| 后勤·校园服务 | `houqin.qdu.edu.cn/fwzn/xyff.htm` | 通勤班车线路时刻表、快递点营业时间+电话、浴室/开水房位置+开放时间、邮政收发室 |
| 后勤·物业服务 / 能源服务 / 教室场馆 | `houqin.qdu.edu.cn/fwzn/wyfw.htm` 等 | 设施、水电、场馆信息 |
| 后勤·一卡通服务 | `houqin.qdu.edu.cn/fwzn/yktfw.htm` | 充值点、办卡补卡 |
| 后勤·通知公告 | `houqin.qdu.edu.cn/index/tzgg.htm` | 食堂供货评审（每月更新）、迎新/节日活动、供暖通知，**带日期** |
| 采购中心 | `cg.qdu.edu.cn` | 食堂招标/中标 → 发现餐厅经营方变更 |
| 学校主站 | `www.qdu.edu.cn` | 要闻、通知公告、校历 |

**社区源（体验类，需谨慎使用）**

- `colleges.chat`（大学生活质量指北）：结构化 Q&A（宿舍/食堂/浴室/消费），**可直接抓取**，信息干净
- 百度贴吧、知乎、豆瓣小组：**直接抓取会被 403 拦截**，只能用 `websearch` 间接搜取**带日期的摘要**，且仅作体验类补充，需交叉验证

**采集规则**

1. 官方源信息可直接采信；社区源每条须标注来源与时间，矛盾处标「待核实」
2. 不确定的信息不编造，无法核实就保留「待更新」
3. 生活板块采集频率建议**每年迎新季**（8-9 月）做一次全量刷新
4. 采集生成的**待更新草稿**统一放 `docs/live/drafts/`，**不加入 `mkdocs.yml` 的 nav**（沿用 3.6「未完成不展示」规则）；Agent 依据草稿更新正式页面后删除草稿
5. 草稿开头加 `!!! warning "待更新草稿"`，正文逐条标注信息来源与采集日期

> 目前该流程尚未固化为可一键触发的自定义命令（计划为 `.opencode/command/collect-sources.md`），可先在维护任务中按上表手动执行采集。

---

## 五、Agent 维护工作流（必读）

每次维护任务，请严格按以下流程执行：

### 第 1 步：通读本文档与上下文

- 完整阅读本文档（AGENT-GUIDE.md）
- 阅读 `mkdocs.yml` 了解当前导航结构
- 阅读与任务相关板块的现有页面，理解其风格与结构

### 第 2 步：定位信息源

- 若维护指令来自 `prompt.md`，先读取其中的需求
- 若维护指令引用了图片/文档，先提取文字内容并归纳

### 第 3 步：设计改动方案

- 明确「改哪个文件 / 新增哪个文件 / 是否动导航」
- 尽量**复用现有结构与样式**，不要引入新概念或新组件（除非维护者明确要求）
- 若需新增页面，决定它在 `mkdocs.yml` 导航中的位置

### 第 4 步：执行修改

- 只改动本次任务相关的文件，**不顺手重构无关内容**
- 保持中文排版规范、表格对齐、提示框语法正确

### 第 5 步：本地验证

在项目根目录执行构建验证，确保没有错误：

```powershell
python -m mkdocs build --strict
```

- 若输出 `INFO - Documentation built in X seconds` 且无 warning/error，说明构建通过
- 有 warning 也要修复（`--strict` 会把警告当错误）
- 构建产物 `site/` 是自动生成的，**不需要也不应该手动修改或提交**

### 第 6 步：复查与提交（若被要求提交）

- 复查改动：diff 是否只包含预期修改
- 提交信息遵循 README 规范：`<类型>(<文件名>): 干了什么`，类型有 `feat`/`fix`/`refactor`/`revert`
- 提交前检查 `git status`，避免误提交临时文件或敏感信息

---

## 六、质量红线（避免「一次失误搞崩网站」）

以下规则是**硬性红线**，违反任一条都可能破坏网站，请务必遵守：

1. **永远先构建再交付**：任何修改后都必须执行 `python -m mkdocs build --strict`，确认通过后才算完成。
2. **改 `mkdocs.yml` 时小心缩进**：YAML 缩进错误会直接导致构建失败。新增导航项务必与同级已有项缩进一致。
3. **不要删除或重排已有导航项**：只做追加；确需调整时应最小化变更。
4. **相对路径必须正确**：站内链接用相对路径，写错会 404。改文件位置时同步改所有引用它的链接。
5. **不破坏特殊语法**：admonition、`md_in_html` 块、表格、代码块都必须闭合正确；`<div class="grid cards">` 等 HTML 块尤其要注意结构完整。
6. **不引入外部依赖**：不要新建需要额外 JS/CSS/插件的功能（除非明确要求）。现有功能的改动要尽量在不破坏现有结构的前提下完成。
7. **不改 `site/` 目录**：它是构建产物，会被 CI 覆盖。
8. **信息准确性优先**：不确定的信息不要编造；无法核实的标「待更新」。特别是官方文件（文号、时间、电话）必须与信息源一致。
9. **不提交敏感信息**：个人手机号、身份证号、家庭住址等敏感信息要脱敏或仅在必要场景保留（校园公开电话除外）。
10. **修改文档类说明时保持一致**：如果更新了页面结构/命名规则，记得同步更新本文档或相关维护说明。

---

## 七、历次维护记录

> 每次完成一次**有意义的维护任务**后，Agent 应在本节追加一条记录，说明改了什么、为什么改、涉及哪些文件。这样后续的 Agent 能快速了解站点演变历史，避免重复劳动或误判。记录格式如下：

```markdown
### YYYY-MM-DD：一句话标题

- **任务**：描述本次维护要解决什么
- **改动**：列出涉及的文件（新增/修改）
- **说明**：关键决策与原因
```

---

### 2026-07-11：规则文档更名 + 新增「未完成维护学院」占位规则

- **任务**：将维护说明由中文名改为英文名（`面向Agent的维护说明.md` → `AGENT-GUIDE.md`），更规范简洁；同时把「未完成维护的学院页」处理方式固化为规则。
- **改动**：
  - 新增/修改：`AGENT-GUIDE.md`（原名 `面向Agent的维护说明.md`，完成更名与全文自引用更新）、`AGENTS.md`（同步更新 3 处指向新文件名的链接）
  - 修改：`mkdocs.yml`（从 nav 移除德雷克联合学院、教育科学学院、艺术学院三个未维护学院项）、`docs/college/index.md`（从索引列表移除这三个学院的占位行）
  - 维持不变：`docs/college/drake_college.md`、`docs/college/education_science.md`、`docs/college/art.md` 三个文件仅作占位，不进入导航与索引
- **说明**：为后续维护预留的学院页只新建文件、不加入 nav 与索引，避免访问者点进空白页；学院板块中医学部→青岛医学院、环境科学与工程学院→环境与地理科学学院已按信息源更名。

---

### 2026-08-02：新增「自主采集信息源」规则（生活板块信息老化应对）

- **任务**：生活板块（餐饮/宿舍/交通等）信息源很老、缺少维护者，需要让 Agent 能自己联网找到大量新鲜信息源，而不依赖人工投喂。
- **改动**：`AGENT-GUIDE.md` 新增 4.3 节「自主采集信息源」，收录**已实测可用**的信息源清单（官方源：后勤官网各服务页/通知公告/采购中心/学校主站；社区源：colleges.chat、贴吧/知乎/豆瓣），并定义采集规则（官方优先、社区源标注来源与日期、草稿放 `docs/live/drafts/` 不入 nav、迎新季全量刷新）。
- **说明**：探测发现贴吧 PC/wap、知乎直接抓取均 403，须用 `websearch` 间接取摘要；官网「校园服务」页其实藏有班车时刻、快递点、浴室等生活干货。采集命令 `.opencode/command/collect-sources.md` 计划后续实现，暂先以文档规则落地。

---

### 2026-08-02：生活板块首次自主采集更新（官方源）

- **任务**：按 4.3 节方法采集官方信息源，更新老化的生活板块页面；宿舍页加入俏皮话。
- **改动**：
  - 修改：`docs/live/eat.md`（食堂总数 21 个、官方营业时间、膳食部电话）、`docs/live/dorm.md`（新建宿舍 A/B/C 三栋 4847 床位 2025-2027，更正旧说法 + 插入 quote 俏皮话）、`docs/live/traffic.md`（新增校内通勤班车/区间车章节）、`docs/live/facilities.md`（官方浴室开放时间与位置）、`docs/live/shopping.md`（官方快递点营业时间与电话）
  - 新增：`docs/live/drafts/information-sources-2026.md`（采集草稿，不入 nav）
- **说明**：坚持「只更新官方确认信息」红线——仅采用后勤官网（饮食/校园/一卡通/能源服务页）与官方公示数据；社区源（贴吧/知乎）信息过脏且无法核实，本次未改动任何社区类内容。草稿归档了后续可用的官方素材（一卡通充值点、能源电话、邮政收发室），供下一次维护使用。

---

### 2026-08-03：首页首屏加载性能优化（四季背景图 WebP 化）

- **任务**：刚进主页较卡、主页大卡片背景图加载时间长、首次进入需长缓冲，定位为背景图体积过大 + 四季切换导致的「首屏双下载」。
- **改动**：
  - 修改：`docs/assets/spring.webp`、`summer.webp`、`autumn.webp`、`winter.webp`（由同名 PNG 转码：缩至 1600px 宽 + WebP q74，4 张合计约 1.55MB → 483KB，-67%）；删除同名 `.png`
  - 修改：`docs/index.md`（hero-bg 背景图 `src="assets/spring.png"` → `.webp`）
  - 修改：`docs/javascripts/season-switcher.js`（`img[data-seasonal]` 背景图扩展名 `.png` → `.webp`；页眉 logo 仍用 `logo-*.png` 不变）
  - 未改动：`mkdocs.yml`（`logo: assets/logo-spring.png` 保持不变）
- **说明**：入口 `index.md` 写死初始 `spring.png`，而季节脚本以 `defer` 方式在 DOM 解析后才执行，非春季访客首屏会同时下载默认图与目标季节图两张背景大图，加上图片本就偏大，故首屏卡。转 WebP 后单张降至约 100–147KB，即使用户切换季节最多也只多一次轻量请求，首次进入体验显著改善；所有改动仅涉及图片编码与引用扩展名，未改变任何展示内容与页面结构。

---

### 2026-08-03：新增主页访问统计滚动栏（vercount）

- **任务**：新增访问记录功能——记录全站访问量与独立访客数，并在主页顶部做一条横向滚动的统计栏动画展示。
- **改动**：
  - 修改：`mkdocs.yml`（`extra_javascript` 新增本地 `javascripts/visit-counter.js` 与第三方统计脚本 `https://vercount.one/js`，全站引入）
  - 新增：`docs/javascripts/visit-counter.js`（轮询读取统计值、填充滚动栏数字、sessionStorage 缓存以兼容 `navigation.instant` 返回主页回填、统计服务不可用时降级为占位符）
  - 修改：`docs/index.md`（主页 Hero 上方新增 `visit-bar` 横向跑马灯 + 隐藏的 `busuanzi_value_site_pv/uv` 统计锚点）
  - 修改：`docs/stylesheets/extra.css`（新增 `visit-bar` 滚动动画样式与 720px 以下小屏适配）
- **说明**：GitHub Pages 为纯静态托管，无法自行获取访问者 IP，维护者选择「免注册现成静态站计数器」路线。官方不蒜子（busuanzi.ibruce.info）近年多次 502/挂服且 Referrer 统计法在移动端不准，故采用社区广泛推荐、无缝兼容不蒜子标签的 Vercount（vercount.one）：UV 按 IP + UserAgent 去重、PV 按页面计数，速度快且稳定。该服务只有累计维度（无「今日」字段），滚动栏展示「累计独立访客 + 累计访问量」；统计不可用时仅显示占位符，不影响页面布局与既有功能。

---

### 2026-08-03：访问统计展示改版（跑马灯 → 炫酷数字卡片）

- **任务**：维护者对滚动栏方案不满意，改为「一眼看清统计」的炫酷卡片式展示，并兼顾手机端窄屏适配。
- **改动**：
  - 修改：`docs/index.md`（移除 `visit-bar` 跑马灯；改为 `visit-stats` 统计卡片：标题行 + 独立访客/累计访问两张卡片）
  - 修改：`docs/stylesheets/extra.css`（移除 `visit-bar` 滚动样式；新增 `visit-stats` 玻璃拟态卡片、金色渐变数字、顶部霓虹高光、hover 微升，及 720px 以下小屏 grid 自适应）
  - 修改：`docs/javascripts/visit-counter.js`（读取改用 `data-count`；数字新增 count-up 滚动动画与千分位格式化）
- **说明**：数据链路不变（仍由 Vercount 填充 `busuanzi_value_*`）。展示改为固定横排双卡片、不再滚动，桌面手机均一目了然；数字加载时从 0 滚动到目标值增加炫酷感。隐藏统计锚点保留。

---

### 2026-08-03：主页统计卡片移入 Hero 下方 + 学科/专业详情弹窗 + 首屏缓冲优化

- **任务**：将访问统计卡片从 Hero 上方移到大卡片下方，优先呈现大卡片；把 Hero 内 3 个统计数字（ESI 1‰ / 1% / 国家一流专业）做成可点击卡片，点击弹出各名单详情；并缓解「数字和图片加载慢」的缓冲问题。
- **改动**：
  - 修改：`docs/index.md`（`visit-stats` 从 Hero 上方移到 Hero 闭合标签之后；Hero 内 3 个 `.stat` 由 `<div>` 改为可点击 `<button data-stat>` + 新增 `hero-stats__hint` 提示；Hero 下方新增 `stats-modal` 弹窗骨架）
  - 新增：`docs/javascripts/stats-modal.js`（点击统计卡片弹出名单；ESI 1‰ 4 个、ESI 1% 17 个、39 个国家一流专业的名单以 JS 数组维护，减少 HTML 体积；兼容 `navigation.instant`）
  - 修改：`docs/stylesheets/extra.css`（新增 `stats-modal` 玻璃拟态弹窗样式与 720px 小屏 2 列适配；`.stat` 补充 button 默认样式重置：cursor/字体/颜色/高亮）
  - 修改：`mkdocs.yml`（`extra_javascript` 移除阻塞首屏的 `https://vercount.one/js` 直引，改由本地脚本异步注入）
  - 修改：`docs/javascripts/visit-counter.js`（`injectVercount()` 用 `async` 动态注入 Vercount，避免阻塞首屏；轮询上限 12s→30s 以覆盖异步加载延迟）
- **说明**：名单数据严格按官方口径（ESI 1‰ 截至 2026-05 四个学科、1% 截至 2025-11 十七个学科、39 个国家一流专业为 2019/2020/2021 三年度共 39 个）。Vercount 改为异步注入不阻塞首屏渲染，数字经 sessionStorage 缓存并在脚本就绪后由轮询更快回填。构建 `python -m mkdocs build --strict` 通过。


### 2026-08-04：学习学业板块「学业制度」改版 + 新增「第二课堂」「创新实践学分」两专栏

- **任务**：基于 temp 目录投递的官方文件（《青岛大学“第二课堂成绩单”制度实施办法》、学时配备表、参与/兑换/审批操作指引、《本科生创新实践学分认定与管理办法》〔青大教字〔2024〕22号〕及计分标准、2021级认定通知等），整理学习学业板块。将「学习制度」栏目更名为「学业制度」，在该栏目页末尾新增「第二课堂」「创新创业学分（创新实践学分）」两块知识入门内容，各自配俏皮的删除线，并分别创建独立的「第二课堂」「创新实践学分」专栏页承载完整规则。
- **改动**：
  - 修改：mkdocs.yml（学习学业板块「学习制度」→「学业制度」并上移为首位；新增第二课堂、创新实践学分两个一级栏目）
  - 修改：docs/study/system.md（H1 与标题改名学业制度；底部新增「第二课堂」「创新创业学分」两个知识点小节 + 俏皮话 + 跳转专栏链接）
  - 新增：docs/study/second_classroom.md（新建第二课堂专栏：是什么 / 意义目的 / 六大类 / 学时学分制 / 学时换算 / 兑换标准速览 / 平台操作指南 / 常见问题）
  - 修改：docs/study/innovation_credit.md（补全文件依据与印发日期、意义目的、认定流程、计分标准补充「文艺创作作品」类、开头俏皮话 + 常见问题）
  - 修改：prompt/AGENT-GUIDE.md（追加本维护记录）
- **说明**：两栏目标签按投递文件的正式名保留「创新实践学分」（青大教字〔2024〕22号），但学习制度入口用「创新创业学分（创新实践学分）」便于新生理解它是第二课堂六大类里创新创业类的 3 个学分。俏皮话用删除线语法（pymdownx.tilde）实现「划去」效果；「驾照加分」等传闻在常见问题中明确并非现行文件项目，避免误导。构建 python -m mkdocs build --strict 通过。

---

### 2026-08-06：修复「返回主页统计卡片空白」问题（instant 导航下数据加载兜底）

- **任务**：用户反馈在其他页面停留后再返回主页，独立访客/累计访问两张卡片无法加载显示。定位为 `navigation.instant` 切换下统计数据获取链路无兜底。
- **改动**：修改 `docs/javascripts/visit-counter.js`——新增直连 Vercount API 的刷新能力（`fetchVisit()`、`refresh()`、与官方一致的 UV cookie 去重），`DOMContentSwitch` 检测到主页锚点即触发刷新；首次加载链路（异步注入 Vercount + 锚点轮询）保持不变。
- **说明**：根因有三层叠加——① Vercount 官方脚本仅在整页加载时执行一次，且用闭包一次性抓取锚点引用、无定时器，instant 返回主页后新 DOM 的隐藏锚点为空白；② 本站锚点轮询只在首次运行约 30 秒后即永久停止（`MAX_TRIES` + `started` 标志阻止重启）；③ 返回主页仅靠 sessionStorage 缓存回填，缓存缺失即永久停留占位符。「待久了」正是让首次轮询窗口在回主页前关闭、且首次进站非主页时无缓存的情况。修复后返回主页先 `restore()` 缓存立即回填避免空白闪烁，再直连 `events.vercount.one/api/v2/log` 拉取最新值并更新缓存，5 秒节流防快速往返连发；每次返回主页的计数请求与「页面被查看」语义一致（此前 instant 切换本就不计数，属口径修正而非虚增），UV 沿用官方同款一年期 cookie 去重不重复计数。**缓冲保障**：所有请求均为异步 fetch、不阻塞渲染；首次加载仍由注入脚本计数、不额外发请求，未引入任何阻塞与外部依赖。`node --check` 与 `python -m mkdocs build --strict` 均通过。

---

### 2026-08-05：首页首屏加载进一步优化 + 「站点热度」栏视觉改版（并评估放弃「在线人数」功能）

- **任务**：主页进入仍有卡顿；「这是我们的热度」统计栏样式不佳；同时评估能否新增「同时在线人数」功能。
- **改动**：
  - 修改：`docs/index.md`（`hero-bg` 由 `<img src="assets/spring.webp">` 改为 `<div data-seasonal>` 动态背景；访问统计由底部独立区块移入 **Hero 主视觉页 CTA 下方**（`hero-visits`：独立访客 `data-count="uv"` 与累计访问 `data-count="pv"` **左右两列并排，每列自上而下 图标→金色数字→浅色释义**，Vercount 隐藏统计锚点一并移入 Hero 内；删除原 `visit-stats` 区块）
  - 修改：`docs/javascripts/season-switcher.js`（`[data-seasonal]` 由「替换 img src」改为「设置 CSS background-image」，并新增脚本执行后立即调用一次 `scheduleApply()`）
  - 修改：`docs/stylesheets/extra.css`（`.hero-bg` 由 img 样式改为背景图样式：`background-size: cover` + 深紫兜底色 `#241d4d`；`hero-zoom` 动画 12s→8s、起幅 scale 1.12→1.08；新增 `.hero-visits` 与改造 `.stat` 为**透明毛玻璃卡片**（半透明 `rgba(255,255,255,0.08)` + `backdrop-filter: blur(10px)` + 细白边框 + 顶边微光 + 柔和阴影，无底色），含 720px 小屏紧凑版与触屏 hover 禁用；删除 `.visit-stats` 全部旧样式）
  - 未改动：`mkdocs.yml`、`visit-counter.js`（数据链路不变，`data-count` / 隐藏锚点结构未动）
- **说明**：
  - **在线人数功能**：调研确认 GitHub Pages 纯静态站无法自研实时在线统计，现有 Vercount 亦只有累计 PV/UV 维度；市面可用的第三方实时服务（Common Ninja、LiveTrafficFeed 等）均需注册、为境外服务、样式不可控且可能拖慢国内访问，与本站「免注册、轻量、国内可访问」原则相悖。经与维护者确认，**放弃该功能**。
  - **卡顿根因**：`index.md` 写死默认 `spring.webp`，而季节脚本会把背景换成当前季节图（8 月即同时下载 spring+summer 两张约 243KB），属「首屏双下载」；且 `hero-zoom` 长达 12s。改为 CSS 背景图后首屏**仅下载目标季节一张图**，动画缩短至 8s，配合深紫兜底色无图也美观。
  - **热度栏改版**：维护者多轮反馈后的最终形态——访问统计从页面底部独立区块**移入 Hero 大卡片主视觉页**（CTA 按钮正下方），保留「独立访客 + 累计访问」双指标（对接 Vercount `site_uv` / `site_pv`），采用**左右两列「透明毛玻璃屏幕卡片」**，每列自上而下「图标→金色数字→浅色释义」，数字小巧（`1.7rem` 等宽、小屏 `1.35rem`），毛玻璃质感通透、与 Hero 深紫背景自然融合；同时把 Hero 上方三个学科卡（`.stat`：ESI 1‰ / 1% / 一流专业）统一为相同的透明毛玻璃样式（去除原紫色渐变底色）。曾尝试的底部独立区块、水平一行并排、上下胶囊行、单数字、带底色卡片等方案均因突兀、单调或不直观被否决。
  - **移动端单屏适配**：为让 Hero 第一页在多数手机上单屏完整显示，720px 以下压缩访问统计卡片（图标 `0.8rem`、数字 `1.1rem`、标签 `0.6rem`，卡片内边距 `7px/6px`，`margin-top: 6px`），并同步收窄第一页垂直间距（`hero-inner` padding 18→14px、`hero-stats` 下边距 18→12px、`hero-subtitle`/`hero-type` 下边距收窄）。
  - 构建 `python -m mkdocs build --strict` 通过。

---

## 八、写给 Agent 的话

本站不是冷冰冰的文档仓库，它承载着学长学姐对学弟学妹的关照。请带着「我是在帮一个新生解决实际困惑」的心态来工作：

- 新生关心的是：去哪个校区报到、宿舍什么样、食堂哪家好吃、怎么选课、考试怎么考、电话打给谁——把信息写到他们最容易找到的地方。
- 能用一句话说清的，不要用一段话。用大白话，不用官腔。
- 你的每一次修改，都会成为某个青大新生大学生活的一部分。请认真对待。

> **维护者寄语**：如果你也觉得曾快乐过，就是最好的结果。

---

## 附：常用命令速查

| 命令 | 用途 |
| --- | --- |
| `python -m mkdocs serve` | 本地预览（http://127.0.0.1:8000） |
| `python -m mkdocs build --strict` | 严格构建验证（Agent 每次必用） |
| `git status` / `git diff` | 复查改动 |
